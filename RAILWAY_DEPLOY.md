# 🚀 Инструкция по деплою на Railway

## Шаг 1: Подготовка проекта

1. Убедитесь, что все изменения закоммичены в Git:
```bash
git add .
git commit -m "Prepare for Railway deployment"
```

2. Создайте репозиторий на GitHub (если еще не создан):
```bash
git remote add origin https://github.com/ВАШ_USERNAME/daily-companion.git
git branch -M main
git push -u origin main
```

## Шаг 2: Настройка Railway

1. Зарегистрируйтесь на [Railway.app](https://railway.app)

2. Нажмите **"New Project"** → **"Deploy from GitHub repo"**

3. Выберите ваш репозиторий `daily-companion`

4. Railway автоматически определит Next.js и установит настройки

## Шаг 3: Переменные окружения

В Railway dashboard, перейдите в раздел **Variables** и добавьте:

```
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://ваш-проект.railway.app
```

## Шаг 4: Deploy Settings

Railway автоматически:
- Установит зависимости (`npm install`)
- Соберет проект (`npm run build`)
- Запустит сервер (`npm run start`)

## Шаг 5: Деплой

1. Railway автоматически задеплоит при каждом push в main ветку

2. Или вручную в dashboard: **Deployments** → **Deploy**

3. После деплоя получите URL: `https://ваш-проект.railway.app`

## Проверка деплоя

Откройте URL и проверьте:
- ✅ Главная страница загружается
- ✅ Dashboard работает
- ✅ Темная тема переключается
- ✅ Язык переключается
- ✅ Все страницы доступны

## Устранение проблем

### Ошибка сборки
- Проверьте логи в Railway dashboard
- Убедитесь, что `package.json` корректен
- Проверьте версию Node.js (должна быть >= 18)

### Страница не загружается
- Проверьте, что порт настроен правильно (Railway автоматически устанавливает PORT)
- Next.js должен слушать на `process.env.PORT || 3000`

### 500 ошибка
- Проверьте серверные логи в Railway
- Убедитесь, что все API routes работают

## Автоматические деплои

Railway настроит автодеплой из GitHub:
- Каждый push в `main` → автоматический деплой
- Pull requests → preview deployments

## Monitoring

В Railway dashboard доступны:
- 📊 Metrics (CPU, Memory, Network)
- 📝 Logs (real-time)
- 🔄 Deployment history
- ⚡ Performance metrics

## Стоимость

- Free tier: 500 часов/месяц ($5 кредит)
- Достаточно для демо и MVP
- При превышении - автоматический upgrade

## Следующие шаги

1. Настроить custom domain (опционально)
2. Добавить PostgreSQL database (когда понадобится)
3. Настроить Redis для кеширования (опционально)
4. Добавить Spotify OAuth (когда будете готовы)

Готово! Ваше приложение должно быть доступно по адресу Railway! 🎉
